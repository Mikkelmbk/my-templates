const message = require('./message'); // import all exported data from the message file and store it in the message variable in this file.


message.logMessageToConsole("Another World"); // we can now call all the functions exported from the message.js file, in here by refering to the variable we stored the required data, followed by the name the function was exported with.